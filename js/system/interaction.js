// js/system/interaction.js
import { GameState } from './state.js';
import { PlayerData, Inventory } from '../data/db.js';
import { Log, updatePlayerStats } from './ui.js';

export const DialogueSystem = {
    // 這裡貼入 DialogueDB 與相關對話函數 (start, renderNode, close)
    // 並在購買項目時呼叫 updatePlayerStats(playerRenderer)
};